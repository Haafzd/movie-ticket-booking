import React from 'react';
import { Container, Typography } from '@mui/material';

const MyTicketsPage: React.FC = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h3">Tiket Saya</Typography>
    </Container>
  );
};

export default MyTicketsPage;
