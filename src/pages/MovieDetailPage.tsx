import React from 'react';
import { Container, Typography } from '@mui/material';

const MovieDetailPage: React.FC = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h3">Detail Film</Typography>
    </Container>
  );
};

export default MovieDetailPage;
